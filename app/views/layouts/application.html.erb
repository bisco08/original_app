<!DOCTYPE html>
<html>
  <head>
    <title>OriginalApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <header class="header">
     <div class="header_bar">
       <h1 class="grid-6"><a href="/">あなたの隠れおすすめスポット！</a></h1>
     </div>
     <% if user_signed_in? %>
       <div class="log_right">
         <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: :log_btn %>
         <%= link_to "新規投稿", new_post_path, class: :log_btn %>
       </div>
     <% else %>
       <div class="log_right" %>
         <%= link_to "ログイン", new_user_session_path, class: :log_btn %>
         <%= link_to "新規登録", new_user_registration_path, class: :log_btn %>
       </div>
     <% end %>
    </header>
    <%= yield %>
   
  </body>
</html>
